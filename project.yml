# Swoosh Xcode Project — Generated by XcodeGen
#
# The previous Apple UI was removed. This project now contains the rebuilt
# macOS onboarding slice plus a universal iPhone/iPad companion.

name: Swoosh
options:
  bundleIdPrefix: ai.swoosh
  deploymentTarget:
    macOS: "26.0"
    iOS: "26.0"
  xcodeVersion: "26.0"
  groupSortPosition: top
  createIntermediateGroups: true
  defaultConfig: Debug

settings:
  base:
    SWIFT_VERSION: "6.3"
    MACOSX_DEPLOYMENT_TARGET: "26.0"
    IPHONEOS_DEPLOYMENT_TARGET: "26.0"
    PRODUCT_BUNDLE_IDENTIFIER: ai.swoosh.$(PRODUCT_NAME:rfc1034identifier)
    DEVELOPMENT_TEAM: GY5597YK9P
    CODE_SIGN_STYLE: Automatic
    ENABLE_APP_SANDBOX: false
    MARKETING_VERSION: "0.5"
    CURRENT_PROJECT_VERSION: "1"

packages:
  FluidAudio:
    url: https://github.com/FluidInference/FluidAudio.git
    from: 0.14.7

targets:
  Swoosh:
    type: application
    platform: macOS
    sources:
      - path: App
    settings:
      base:
        PRODUCT_NAME: Detour
        PRODUCT_BUNDLE_IDENTIFIER: ai.swoosh.detour.mac
        GENERATE_INFOPLIST_FILE: true
        INFOPLIST_KEY_CFBundleDisplayName: Detour
        INFOPLIST_KEY_LSUIElement: false
        INFOPLIST_KEY_NSMainNibFile: ""
        INFOPLIST_KEY_NSHumanReadableCopyright: ""
        INFOPLIST_KEY_NSLocalNetworkUsageDescription: Detour uses your local network to let your iPhone connect to this Mac.
        INFOPLIST_KEY_NSMicrophoneUsageDescription: Detour records a short local voice sample so it can recognize your voice.
        INFOPLIST_KEY_NSSpeechRecognitionUsageDescription: Detour uses local speech recognition to detect the wake word you choose.

  SwooshiOS:
    type: application
    platform: iOS
    sources:
      - path: Apps/SwooshiOS
      - path: Sources/SwooshClient
    dependencies:
      - package: FluidAudio
        product: FluidAudio
    settings:
      base:
        PRODUCT_NAME: Detour
        PRODUCT_BUNDLE_IDENTIFIER: ai.swoosh.app.ios
        INFOPLIST_FILE: Apps/SwooshiOS/Info.plist
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTS_MACCATALYST: false

schemes:
  Swoosh:
    build:
      targets:
        Swoosh: all
    run:
      config: Debug

  SwooshiOS:
    build:
      targets:
        SwooshiOS: all
    run:
      config: Debug
