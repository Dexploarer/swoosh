# Swoosh Naming Rules

- Write elizaOS exactly as `elizaOS`.
- Write npm packages under the lowercase `@elizaos/*` scope.
- Say `Eliza agents` in prose, not `elizaOS agents`.
- Keep Swoosh module names in PascalCase and target names aligned with `Package.swift`.
