# /apple-test

Create or run the right tests.

Include:

- Swift Testing for pure logic and tool schemas.
- XCTest for integration where project already uses XCTest.
- XCUIAutomation for user flows, permissions, shortcuts, App Clip, widget launch paths.
- Prompt/tool evals for AI features.
- Build command and simulator/device target.
- Failure summary and next fix.
