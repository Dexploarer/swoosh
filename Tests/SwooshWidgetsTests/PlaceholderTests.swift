// SwooshWidgetsTests/PlaceholderTests.swift — placeholder so the declared
// test target compiles. Add real tests as the widget API grows.

import XCTest
@testable import SwooshWidgets

final class SwooshWidgetsPlaceholderTests: XCTestCase {
    func testTargetLinks() {
        XCTAssertTrue(true)
    }
}
